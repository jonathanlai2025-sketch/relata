# LOCALITY-EMERGENCE-01

Frozen ticket. Authoritative gate. Do not optimize toward three dimensions, gravity, a lattice, or PASS.

**Level 1 is the prize. It is not in hand. Levels 3–5 are not entitled to comment yet.**

## Question

Does a robust finite-dimensional operational-locality phase exist in a system whose microscopic rules contain no metric coordinates or background geometry?

A pass means a connected, persistent, finite-dimensional, approximately metric operational phase — not an attractive visualization. A pass earns the causal-structure question. It does not establish spacetime or gravity.

## Terminal outcomes (exactly one)

| Verdict | Meaning |
|---|---|
| `PASS` | Conjunction of all load-bearing predicates is true. |
| `FAIL` | Instrument was valid and the conjunction is false. Failed candidate mechanisms stay FAIL. |
| `INSTRUMENT_INVALID` | A static positive control failed persistence/probe. Not evidence against the conjecture. |

Coarse ledger: **NO PASS**.

Promotion rule: **PASS iff every load-bearing predicate passes.** No weighted score. No majority vote. No “promising.” No rescuing a failed gate with a beautiful phase diagram.

## Conjunction

\[
\boxed{ \mathrm{PASS} = N \land C \land Q \land T \land D \land M \land X \land A \land S \land R }
\]

This extends the original G1–G6 battery; it does not replace it.

| ID | Predicate | Notes |
|---|---|---|
| \(N\) | No smuggling | No coordinates, target \(D\), \(1/r\), planted lattice, 2-hop proposal. |
| \(C\) | Connectedness | G1. Protocol-frozen: giant \(\ge 0.80\). |
| \(Q\) | Cross-probe | Intervention \(M\), correlation, and hitting/first-arrival. Changing source node is not enough. **Overlap cutoff uncalibrated.** |
| \(T\) | Persistence | G3. Protocol-frozen: Jaccard \(\ge 0.35\). |
| \(D\) | Finite-dimensional | G4. Ball growth **and** spectral return, independently. Do not force 3 or 4. |
| \(M\) | Approximate metric | G5. Protocol-frozen: triangle \(\ge 0.70\). |
| \(X\) | Non-degeneracy **and** control separation | G6 plus the experimental matrix: mean-field, expander/random-regular, frozen skeleton, null dynamics. Expander is one adversary, not the whole control set. |
| \(A\) | Ablation | Capacity off, Hebb off, shuffle partners. A claimed phase must collapse. |
| \(S\) | Finite-size / phase scaling | **Drift cutoff uncalibrated.** |
| \(R\) | Independent replication | Different seed and implementation. |

## Do not fabricate thresholds

Protocol G1–G6 numbers were frozen in Experiment Zero before looking. New numbers (probe overlap, \(D_{\mathrm{eff}}\) drift) are **not** justified by the corpus. Calibrate them on known positive and negative controls, freeze them, then look at the candidate. Choosing 0.80 or 5% now is statistical smuggling.

A naive single \(M_{i\to j}\) threshold does not cleanly distinguish a cycle from random-regular. That sanity check is **not** Experiment Zero evidence. It is why \(Q\) and the full matrix are required.

## Estimators (runnable; cutoffs for Q/S not frozen)

\[
M_{i\to j}(t)=D_{\mathrm{TV}}\big[P(s_j(t)\mid \mathrm{do}[s_i=0]),\, P(s_j(t)\mid \mathrm{do}[s_i=1])\big]
\]

Independent probes must recover compatible neighborhoods. Dimension from influence-ball/Hausdorff growth and from spectral return — neither forced toward 3 or 4.

## Three ledgers (do not mix)

| Ledger | Content |
|---|---|
| CONJECTURE | relationship → locality → geometry → perhaps eventually gravity |
| EXPERIMENT | Can coordinate-free relational dynamics produce robust operational locality? |
| EVIDENCE | **NO PASS.** Failed mechanisms: FAIL. First-run static \(T\): INSTRUMENT_INVALID. |

## Level lock

| Level | State |
|---|---|
| 1 Operational locality | OPEN / NOT ESTABLISHED |
| 2 Universality-class hint | LOCKED |
| 3 Kinematics | LOCKED |
| 4 Effective physics | LOCKED |
| 5 Gravity | LOCKED |

Mutual information, \(1/I\) distance, entropy maximization, Lorentz emergence, and gravitational behavior remain downstream possibilities, not derived facts.

## Next object

Not another theory discussion. A frozen executable evidence run, after Q/S cutoffs are calibrated on controls and frozen **before** inspecting the candidate.

Until that comes back green:

**Level 1 is the prize. It is not in hand.**
